import React from "react";
import "./Header.css";
import NavBar from "../Navbar/NavBar";

const Header = () => {
  return (
    <header id="header">
      <NavBar />
    </header>
  );
};

export default Header;
